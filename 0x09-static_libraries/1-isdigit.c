#include "main.h"

/**
 * _isdigit - it looks for a digit
 * @c: the int that will be checked
 * Return: 1 if c is a digit and 0 if it is not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
