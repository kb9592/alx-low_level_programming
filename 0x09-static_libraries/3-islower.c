#include "main.h"
/**
 * _islower - this checks for characters in lowercase
 * @c: the character to check
 * Return: 1 if c is lowercase and 0 if it is not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
