#include <stdio.h>
/**
 * main - entry for a program that prints the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
